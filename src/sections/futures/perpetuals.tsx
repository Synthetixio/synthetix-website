import { Section } from 'src/styles/common';
import styled from 'styled-components';

export default function Perpetuals() {
	return <PerpetualsSection>hey</PerpetualsSection>;
}

const PerpetualsSection = styled(Section)``;
